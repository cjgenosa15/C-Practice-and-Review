/*
 * POINTER PRACTICE - ALL CHALLENGES
 * Challenge 1: findLargest(a, b, c, largest) 
 *              - find largest of 3 numbers, store in 4th pointer
 * Challenge 2: swapNoTemp(a, b)
 *              - swap two numbers WITHOUT temp variable, math only!
 *              - hint: *a=*a+*b, *b=*a-*b, *a=*a-*b
 * Challenge 3: distance(a, b, result)
 *              - find absolute difference, store in 3rd pointer
 *              - hint: if *a>*b → result=*a-*b, else result=*b-*a
 */

#include<stdio.h>

int main(){

    return 0;
}